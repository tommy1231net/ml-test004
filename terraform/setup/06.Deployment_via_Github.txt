# Run the following command in the folder containing main.py.

gh auth login
# Select "GitHub.com" and "HTTPS," then choose "Login with a web browser.
# Follow the on-screen instructions to log in to GitHub.

git init
git add .
git commit -m "First Deploy"
git branch -M main
# Replace ”XXXXX" to GitHub Repository Name
git remote add origin https://github.com/XXXXX/YYYYY.git
git push -u origin main --force



